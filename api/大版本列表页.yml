name: 大版本列表页
base_url: ${ENV(url)}
variables:
    JSESSIONID: ${ENV(JSESSIONID)}
    page:
    pageSize:
request:
    url: /admin/version/getVersionPageList
    method: POST
    headers:
        Cookie: JSESSIONID=$JSESSIONID
        Content-Type: application/x-www-form-urlencoded; charset=UTF-8
    params:
        page: $page
        pageSize: $pageSize
validate:
    - eq: ["status_code", 200]