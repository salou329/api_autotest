name: 版本详情列表页
base_url: ${ENV(url)}
variables:
    JSESSIONID: ${ENV(JSESSIONID)}
    vid:
    page:
    pageSize:
request:
    url: /admin/version/getVersionDetailPageList
    method: POST
    headers:
        Cookie: JSESSIONID=$JSESSIONID
        Content-Type: application/x-www-form-urlencoded; charset=UTF-8
    params:
        vid: $vid
        page: $page
        pageSize: $pageSize
validate:
    - eq: ["status_code", 200]