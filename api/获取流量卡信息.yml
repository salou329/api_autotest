name: 获取流量卡信息
base_url: ${ENV(url)}
variables:
    id: id
	JSESSIONID: ${ENV(JSESSIONID)}
request:
    url: /admin/device/getSIMCardInfo
    method: POST
    headers:
        Cookie: JSESSIONID=$JSESSIONID
        Content-Type: application/x-www-form-urlencoded;
    params:
        id: $id
validate:
    - eq: ["status_code", 200]
