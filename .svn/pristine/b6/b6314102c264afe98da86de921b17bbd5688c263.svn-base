name: 获取设备自检信息列表
base_url: ${ENV(url)}
variables:
    id: id
	JSESSIONID: ${ENV(JSESSIONID)}
request:
    url: /admin/devicedetectlog/index
    method: POST
    headers:
        Cookie: JSESSIONID=$JSESSIONID
        Content-Type: application/x-www-form-urlencoded;
    params:
        id: $id
validate:
    - eq: ["status_code", 200]
