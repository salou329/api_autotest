name: 获取省列表
base_url: ${ENV(url)}
variables:
    JSESSIONID: ${ENV(JSESSIONID)}
request:
    url: /admin/address/china/listProvince
    method: POST
    headers:
        Cookie: JSESSIONID=$JSESSIONID
        Content-Type: application/x-www-form-urlencoded;
validate:
    - eq: ["status_code", 200]
